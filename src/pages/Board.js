import React from 'react';
import {Header,BoardList} from '../components/home';

const Board = (props) => {
    return (
        <>
            <Header/>
            <BoardList/>
        </>
    );
};

export default Board;
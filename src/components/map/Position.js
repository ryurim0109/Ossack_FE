import React from 'react';
import Axios from "axios";



const Position = (props) => {
    const { kakao } = window;
    
    const {pos,map} =props;
    console.log(pos,map)
    let kakaoMap = map;

    return (
        <React.Fragment>
            
        </React.Fragment>
    );
};

export default Position;